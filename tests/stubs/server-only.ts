// Test-time stub for Next.js's `server-only` marker. In production, importing
// this module from a client component throws at build time. Under Vitest we
// just no-op so tests can import server-side code that includes it.
export {};
